function imagemn = bitdropshift(imagem)
%BITDROPSHIFT - PRODUZ A IMAGEM COLORIDA APENAS COM OS 2 BITS MAIS
%SIGNIFICATIVOS DE CADA COR E ACOPLANDO UM AO OUTRO;
%
%imagemn = bitdropshift(imagem)
%
%Dados de entrada:
%imagem - Uma imagem RGB de entrada
%x - Número de bits mais significativos que iremos manter
%
%imagemn - imagem de sa�da em que as cores RGB foram quantizadas,
%utilizando apenas os x bits mais significativos de cada layer RGB.
%
%Uma imagem RGB possui 24 bits, 8 em cada layer, o que gera 256^3 cores.
%Utlizando o bitdropping com 2 bits significativos, teremos uma paleta de
%cores de apenas 64 cores.


imagem1 = bitshift(imagem(:,:,1), -6);
imagem1 = bitshift(imagem1,4);
imagem2 = bitshift(imagem(:,:,2), -6);
imagem2 = bitshift(imagem2,2);
imagem3 = bitshift(imagem(:,:,3), -6);

imagemn = bitor(bitor(imagem1,imagem2),imagem3);